-- Lists all records of "second_table", displaying columns in order of 
-- 'score' and 'name' and the results ordered by 'score'(top first)
SELECT `score`, `name` FROM `second_table` ORDER BY score DESC;
